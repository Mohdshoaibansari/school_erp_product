# Frontend (deferred)

This directory is a placeholder. C-01 is API-first per A8/A9 — no frontend is built
in this change. The Vite + React SPA will be scaffolded when the first UI-bearing
capability is implemented.
