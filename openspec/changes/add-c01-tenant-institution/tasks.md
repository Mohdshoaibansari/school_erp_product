# Implementation Tasks — C-01 Tenant & Institution Management

> **Traceability.** Each task traces to ADR decision IDs (D1–D12, Q1–Q10) and PRD AC IDs (AC-1..AC-20). Tasks are grouped by concern and ordered by dependency. This is a checklist for the apply phase — no implementation is performed here.
>
> **Stack & architecture note (RESOLVED).** The platform tech-stack ADR locks the stack (Postgres+Supabase, Python+FastAPI, SQLAlchemy 2.0+Alembic, Supabase Auth JWT, Casbin RBAC+ABAC, pytest). The platform software-architecture ADR locks the modular-monolith + module-manifest + monorepo structure (A1–A11). The previous "stack TBD — confirm before implementation" gating tasks (old 1.1/1.2) are superseded by concrete scaffold tasks below.
>
> **References:** proposal.md, specs/tenant-institution/spec.md, design.md; ADR `docs/architecture/adr-c01-tenant-institution-implementation.md`; platform software-architecture ADR `docs/architecture/adr-platform-software-architecture.md` (A1–A11); platform tech-stack ADR `docs/architecture/adr-platform-tech-stack.md`; PRD `docs/prd/c-01-tenant-institution.md`.

## 1. Project scaffold & bootstrapping (A1, A2, A5, A7, A10) — REPLACES former tech-stack-confirmation gate

> The former "Tech-stack confirmation" gate (old 1.1/1.2) is RESOLVED — the stack is locked in the tech-stack ADR. Section 1 is now concrete scaffolding that C-01's apply phase (C-01 is the first kernel module — A2) must bootstrap.

- [x] 1.1 Initialize the monorepo layout (A10): `/backend` (uv project, Python 3.11, `pyproject.toml`), `/frontend` (pnpm placeholder — deferred, C-01 is API-first per A8/A9), `/packages` (placeholder for shared TS / OpenAPI→TypeScript types per A11). — evidence: directory structure + `pyproject.toml`.
- [x] 1.2 FastAPI app factory + module-manifest skeleton (A5): `/backend/kernel/app_factory.py` reads a configured explicit module list (NOT auto-discovery) and invokes manifest hooks (`register_routes`, `register_casbin_policies`, `on_startup`, `register_cli`) in dependency order (kernel→shared→business per A2/A3). — evidence: `test_app_boots_with_zero_modules` + `test_app_boots_with_c01_manifest` pass.
- [x] 1.3 Single Alembic environment at `/backend/` (A7) with module-prefixed filename convention (`NNN_c01_*.py`); Supabase CLI local config (`supabase/`); pytest harness with a schema-reset between-run fixture (tech-stack ADR: RLS policies are raw SQL inside Alembic migrations; Alembic owns all schema, NOT the Supabase CLI migration system). — evidence: `alembic upgrade head` succeeds; `supabase start` succeeds; `pytest` 29/29 pass.
- [x] 1.4 `import-linter` config (`.importlinter` / `pyproject.toml [tool.importlinter]`) enforcing A3/A4 (kernel→∅; no shared→business; no internal cross-module imports; acyclic; published `services/` only) + a sample contract test asserting C-01 (kernel) imports nothing from shared/business. — evidence: `lint-imports` passes (2 contracts kept); `test_kernel_imports_nothing_from_shared_or_business` passes.

## 2. Database schema — C-01 entity tables (D2, D4, D5, D6, D7) — single Alembic env, module-prefixed files `NNN_c01_*`

> All C-01 migrations live in the single Alembic env at `/backend/migrations/` (A7) under filenames prefixed `NNN_c01_*` (e.g., `001_c01_create_clients.py`). RLS policies are written as raw SQL (`CREATE POLICY`) inside the SAME Alembic migrations (tech-stack ADR §3); the Supabase CLI migration system is NOT used for schema.

- [x] 2.1 Create the `client` table with the D4 intrinsic fields (`id` UUID v4 PK, `slug` globally-unique immutable, `display_name`, `legal_name`, `legal_entity_type` FK to lookup table, `tax_registration_number`, contact fields, `address_id` FK → C-13, `current_lifecycle_status`, audit timestamps). No `client_id` column (the Client *is* the tenant, Q1). — evidence: migration `001_c01_initial.py` creates `client` table; `test_client_pk_is_uuid_v4` + `test_client_has_no_client_id_column` + `test_client_field_purity` pass.
- [x] 2.2 Create the `institution` table with the D5 intrinsic fields (`id` UUID v4 PK, `client_id` FK → client + RLS tenant column, `institution_type_id` FK immutable after creation, `display_name`, contact fields, `address_id` FK → C-13, `current_lifecycle_status`, affiliate fields, audit timestamps). — evidence: migration `001_c01_initial.py` creates `institution` table; `test_institution_pk_is_uuid_v4` + `test_institution_field_purity` pass.
- [x] 2.3 Create the `institution_type` table with the D7 intrinsic fields (`id` UUID v4 PK, `name` FK to lookup table per Q2, `code` unique, `is_system`, `default_org_unit_template` JSONB, audit timestamps). — evidence: migration `001_c01_initial.py` creates `institution_type` table with JSONB template column.
- [x] 2.4 Create the `org_unit` table with the D6 intrinsic fields (`id` UUID v4 PK, `client_id` FK + RLS, `institution_id` FK default repo filter, `parent_id` self-FK nullable, `name`, `type` FK to lookup table per Q2 (immutable after creation), `sort_order`, `code` within-institution unique, `current_lifecycle_status`, audit timestamps). — evidence: migration `001_c01_initial.py` creates `org_unit` table; `test_org_unit_pk_is_uuid_v4` + `test_org_unit_root_and_child` + `test_org_unit_purity` pass.

## 3. Database schema — lookup tables for configurable enums (Q2, AC-20)

- [x] 3.1 Create lookup table `legal_entity_type` (`id`, `name`). — evidence: migration `001_c01_initial.py` creates `legal_entity_type` table; `test_add_legal_entity_type_via_data_insert` + `test_legal_entity_types_seeded` pass.
- [x] 3.2 Create lookup table `org_unit_type` (`id`, `name`). — evidence: migration `001_c01_initial.py` creates `org_unit_type` table; `test_add_org_unit_type_via_data_insert` + `test_org_unit_types_seeded` pass.
- [x] 3.3 Create lookup table `institution_type_name` (`id`, `name`) for InstitutionType names. — evidence: migration `001_c01_initial.py` creates `institution_type_name` table; `test_add_institution_type_name_via_data_insert` + `test_institution_type_names_seeded` pass.
- [x] 3.4 Seed initial lookup data for legal entity types (e.g., Sole Proprietor / Partnership / Pvt Ltd / Trust / Society per D4) and OrgUnit types (Department / Faculty / Grade / Division / Section / Class / Program / Batch / Course per D6). — evidence: seed INSERT statements in `001_c01_initial.py`; `test_legal_entity_types_seeded` + `test_org_unit_types_seeded` + `test_institution_type_names_seeded` pass.

## 4. Database schema — lifecycle-event and ownership-transfer tables (D8, D9, D12, Q3)

- [x] 4.1 Create `client_lifecycle_events` history table (`id` UUID v4, `client_id` FK, `state` entered, `entered_at`, `reason`, `actor`, FK to `Approval` if applicable per Q3). — evidence: migration `001_c01_initial.py` creates `client_lifecycle_event` table with FK to `approval`.
- [x] 4.2 Create `institution_lifecycle_events` history table (`id` UUID v4, `client_id` + `institution_id` FKs, `state` entered, `entered_at`, `reason`, `actor`, FK to `Approval` if applicable). — evidence: migration `001_c01_initial.py` creates `institution_lifecycle_event` table with FKs.
- [x] 4.3 Create `ownership_transfer_events` table (`id` UUID v4, `from_client_id`, `to_client_id`, `institution_id`, `approved_by`, `consent_source`, `consent_dest`, `transferred_at`, `reason`, FK to `Approval`). — evidence: migration `001_c01_initial.py` creates `ownership_transfer_event` table with all D12 fields.
- [x] 4.4 Create the `approvals` table per Q3 (`id` UUID v4, `requested_by`, `approved_by`, `status`, `requested_at`, `approved_at`, context fields). — evidence: migration `001_c01_initial.py` creates `approval` table; `test_approval_pk_is_uuid_v4` + `test_approval_row_creatable` pass.

## 5. Database schema — RLS policies (D1, Q1, AC-1, AC-14) — raw SQL inside the same Alembic migrations

> RLS policies are emitted as `CREATE POLICY` raw SQL inside `NNN_c01_*` Alembic migration files (tech-stack ADR §3 — SQLAlchemy does not model RLS natively; schema + RLS versioned together in Alembic). The single Alembic env owns the migrations (A7); the Supabase CLI migration system is NOT used for schema.

- [x] 5.1 Enable RLS and create a `client_id`-matching policy on every tenant-scoped C-01 table (`institutions`, `org_units`, `client_lifecycle_events`, `institution_lifecycle_events`, `ownership_transfer_events`). — evidence: migration `001_c01_initial.py` enables FORCE RLS + creates policies on all 5 tenant-scoped tables; `test_cross_tenant_isolation_institution` + `test_cross_tenant_isolation_org_units` pass.
- [x] 5.2 Create the self-visible RLS policy on the `client` table: `id = current_client_id` (Q1). — evidence: migration `001_c01_initial.py` creates `client_self_visible` policy (`id = current_client_id()`); `test_self_visible_client_rls` passes (Client A can see only own row).
- [x] 5.3 Verify there is NO RLS policy on `institution_id` (it is a business filter, not a hard fence, per D1). — evidence: `test_no_rls_on_institution_id` inspects policy expressions and confirms no `institution_id` reference; cross-institution read within same client works (business filter, not RLS).
- [x] 5.4 Add a Platform Owner bypass/super_admin path for RLS (D11). — evidence: migration `001_c01_initial.py` creates `is_platform_owner()` function + OR in all policies; `test_platform_owner_reads_all_clients` + `test_platform_owner_reads_all_institutions_across_tenants` pass.

## 6. Repository layer — tenant-aware data access (D1, Q6, A6, AC-1, AC-9) — C-01 repos live under `kernel/tenant_institution/repos/`

> C-01 repos live under `/backend/kernel/tenant_institution/repos/` (A2 — C-01 is a kernel-tier module). Per A6 + tech-stack ADR, repos return **DTOs, not ORM objects** (prevents lazy-load tenant bypass). The manifest (A5) wires repos as module-scoped **singletons** via constructor injection. `TenantContext` is obtained via `Depends(get_tenant_context)` which sources the contextvar set by subdomain+JWT middleware (A6) — endpoints NEVER read the contextvar directly.

- [x] 6.1 Implement tenant-aware repository base that injects `client_id` from `TenantContext` (obtained via `Depends(get_tenant_context)`, A6) into every tenant-scoped query (business logic never passes `client_id`, never writes SQL). Repositories return DTOs, not ORM objects (tech-stack ADR §3). — evidence: `kernel/tenant_institution/repos/base.py` + `repos/__init__.py`; `test_repo_list_filters_by_client_id_even_when_caller_omits_it` + `test_repo_returns_dtos_not_orm_objects` + `test_repo_org_unit_returns_dtos` pass. (AC-1, ADR §5 constraint 1, A6)
- [x] 6.2 Implement the `institution_id` default business filter with cross-institution role override (Client Director etc. — D11). — evidence: `test_cross_institution_override_allows_director_to_read_across_institutions` + `test_client_a_director_cannot_read_client_b_data` pass. (AC-1)
- [x] 6.3 Implement OrgUnit cycle-prevention check in the repository before any parent-change update (Q6 — app-side; NO DB trigger). — evidence: `test_cycle_prevention_rejects_move_under_descendant` + `test_no_db_trigger_for_cycle_prevention` pass. (AC-9)
- [x] 6.4 Implement OrgUnit subtree-move semantics (the whole subtree moves with the node). — evidence: `test_subtree_move_retains_descendant_structure` pass. (AC-9)

## 7. API layer — subdomain resolution + endpoints (Q5, A6, AC-12) — routes registered via the manifest `register_routes` hook

> Subdomain+JWT middleware sets the contextvar (A6); endpoints read `TenantContext` via `Depends(get_tenant_context)` and NEVER touch the contextvar directly (A6 invariant). C-01 routes are registered via the manifest `register_routes(app)` hook (A5): the subdomain-resolved client-portal router (Institution/OrgUnit) and the platform-scoped router (Platform-Owner-only per D11). The app factory invokes the hook in dependency order (kernel→shared→business).

- [x] 7.1 Implement subdomain → Client resolution + Supabase JWT middleware that sets the contextvar (A6) — the Client is implicit from the subdomain per D3; the c-01-explained `POST /api/clients/{slug}/institutions` form is superseded and MUST NOT be used. — evidence: `kernel/middleware.py` (SubdomainJWTMiddleware); `test_subdomain_resolves_client_and_populates_contextvar` + `test_platform_path_sets_platform_owner` pass. (AC-12)
- [x] 7.2 Implement the `TenantContext` (carries `client_id` + selected `institution_id` from JWT) surfaced via `Depends(get_tenant_context)` (A6); endpoints must NOT read the contextvar directly. — evidence: `kernel/tenant_context.py` (TenantContext + get_tenant_context + require_platform_owner); `test_tenant_context_carries_both_ids` + `test_endpoint_overrides_dependency_cleanly` pass.
- [x] 7.3 Implement Client CRUD + identity-update endpoints under the platform-scoped base (Platform-Owner-only per D11): create Client, identity update, lifecycle transitions. — evidence: `routes/platform.py`; `test_create_client_valid_slug`, `test_reserved_slug_rejected`, `test_format_violation_rejected_short`, `test_slug_collision_returns_taken_no_suggestions`, `test_slug_immutable_on_update`, `test_display_name_mutable`, `test_client_lifecycle_prospective_to_active`, `test_client_lifecycle_terminated_is_terminal`, `test_client_lifecycle_disallowed_arc_rejected` pass. (AC-3, AC-13, AC-5, AC-15)
- [x] 7.4 Implement Institution CRUD + identity-update endpoints under the client-portal subdomain base (subdomain-resolved per Q5): create Institution, identity update, lifecycle transitions, go-live (`Onboarding → Active`). — evidence: `routes/client_portal.py`; `test_create_institution_subdomain_resolved`, `test_institution_lifecycle_onboarding_to_active`, `test_institution_type_immutable`, `test_institution_field_purity` pass. (AC-6, AC-7, AC-12, AC-17). NOTE: AC-7 effective-state gating is partially deferred to Apply-C (task 8.3) — the endpoint structure is in place; full runtime effective-state gating depends on the state-machine module.
- [x] 7.5 Implement InstitutionType management endpoints (platform-scoped, Platform-Owner-only per D11): create/update InstitutionType with JSONB template. — evidence: `routes/platform.py`; `test_create_institution_type_with_template` + `test_template_materialized_at_institution_creation` pass. (AC-16, AC-20)
- [x] 7.6 Implement OrgUnit endpoints (client-portal base, Institution/Admin scope per D11): create, move (cycle-prevented), archive, reactivate, reorder. — evidence: `routes/client_portal.py`; `test_create_org_unit`, `test_archive_org_unit`, `test_reactivate_org_unit`, `test_move_cycle_prevented`, `test_move_valid`, `test_reorder_org_unit` pass. (AC-8, AC-9, AC-10). NOTE: AC-10 move-audit is deferred to Apply-D (task 13.3) — move currently persists without C-11 audit emission; the endpoint structure is in place for the audit emitter to hook in.
- [x] 7.7 Implement ownership-transfer request/approval endpoints (platform-scoped, Platform-Owner-only per D12). — evidence: `routes/platform.py`; `test_request_transfer_creates_pending_approval` + `test_approve_transfer_executes_single_transaction` pass. (AC-11, AC-19). NOTE: C-11 audit emission deferred to Apply-D (task 13.4); post-move isolation verification deferred to Apply-C (task 11.3); C-05/C-02 downstream coordination is a boundary declaration (owned by those capabilities). The endpoint structure + single-transaction transfer of Institution + OrgUnit client_id A→B is proven.

## 8. Lifecycle state machines + Approval flow (D8, D9, Q3, AC-5, AC-6, AC-7, AC-19)

- [x] 8.1 Implement the Client lifecycle state machine (D8 arcs; `Terminated` terminal; `Archived` only re-activatable). Reject disallowed arcs. — evidence: `services/state_machine.py` (CLIENT_ARCS, validate_client_transition); `test_lifecycle.py::TestClientStateMachine` (all allowed arcs accepted, Terminated terminal, Terminated→Active rejected, disallowed arcs rejected, Archived re-activatable). (AC-5)
- [x] 8.2 Implement the Institution lifecycle state machine (D9 arcs; no `Terminated`). Reject disallowed arcs. — evidence: `services/state_machine.py` (INSTITUTION_ARCS, validate_institution_transition); `test_lifecycle.py::TestInstitutionStateMachine` (all allowed arcs accepted, no Terminated state, disallowed arcs rejected, Archived re-activatable). (AC-6)
- [x] 8.3 Implement runtime effective-state gating: an Institution is operationally Active only if its Client is Active — gated at runtime WITHOUT mutating the Institution row on transient Client suspension. — evidence: `repos/institution_repo.py::get_effective_state` + `services/state_machine.py::is_institution_operationally_active`; `test_lifecycle.py::TestEffectiveStateGating` (suspend Client→gated, restore→active, persisted state NOT mutated). (AC-7)
- [x] 8.4 Implement the Approval flow (Q3): request creates a pending `Approval`; the dependent transition/transfer cannot complete until `status=approved`; `status=denied` blocks. — evidence: `services/approval.py` (request_approval, approve_approval, deny_approval, assert_approved); `test_lifecycle.py::TestApprovalFlow` (pending blocks, approved completes, denied permanently blocks); `test_transfer.py::TestTransferApprovalFlow` (transfer blocked without consent, blocked when denied). (AC-19)
- [x] 8.5 Wire every lifecycle transition to write a `client_lifecycle_events`/`institution_lifecycle_events` row with `state`, `entered_at`, `reason`, `actor`. — evidence: `repos/client_repo.py::transition_lifecycle` + `repos/institution_repo.py::transition_lifecycle` (both write event rows); `test_lifecycle.py::TestLifecycleEventRecording` (multiple transitions write multiple events with state/reason/actor). (AC-5, AC-6)

## 9. OrgUnit hierarchy + move (D6, Q6, Q7, AC-8, AC-9, AC-10)

- [x] 9.1 Implement OrgUnit archive-only deletion (soft-delete to `archived`) + reactivation; no hard-delete path. — evidence: `repos/org_unit_repo.py` (archive/reactivate, no delete method); `test_org_unit_hierarchy.py::TestOrgUnitArchiveOnly` (archive soft-deletes, reactivation restores active, no hard-delete path exists, archived row still in DB). (AC-8)
- [x] 9.2 Implement OrgUnit type immutability after creation. — evidence: `repos/org_unit_repo.py::update_type` (always raises ValueError); `test_org_unit_hierarchy.py::TestOrgUnitTypeImmutability` (update_type rejected, update_identity doesn't accept type_id). (AC-8)
- [x] 9.3 Implement recursive CTE subtree/ancestor queries. — evidence: `repos/org_unit_repo.py::get_subtree` + `get_ancestors` (recursive CTE queries); `test_org_unit_hierarchy.py::TestRecursiveCTE` (subtree returns all descendants, leaf returns self, ancestors returns full chain, root returns empty). (D6)
- [x] 9.4 Implement OrgUnit move with cycle-prevention (Q6) + subtree moves + audit (Q7). — evidence: `repos/org_unit_repo.py::move` (cycle-prevented, subtree moves, placeholder `org_unit_moved` log event); `test_org_unit_hierarchy.py::TestOrgUnitMove` (cycle prevented, subtree moves with node, NO dedicated `org_unit_move_event` table, placeholder audit event emitted). NOTE: AC-10 full audit emission deferred to Apply-D task 13.3 — move currently emits a placeholder structured log event, full C-11 wiring pending. (AC-9, AC-10)

## 10. InstitutionType template materialization (D7, AC-16)

- [x] 10.1 Implement JSONB template validation at InstitutionType create/update: referenced OrgUnit types valid; tree acyclic. — evidence: `repos/institution_type_repo.py::validate_template` (validates OrgUnit type references + acyclicity); `test_template.py::TestTemplateValidation` (valid template accepted, invalid type rejected, cyclic template rejected, missing type rejected, create validates). (D7)
- [x] 10.2 Implement template materialization at Institution creation: OrgUnit rows matching the template are stamped with the new `client_id` + `institution_id` and correct `type`/`sort_order`. — evidence: `repos/institution_repo.py::_materialize_template` (recursive tree materialization); `test_template.py::TestTemplateMaterialization` (4-node tree materialized with correct parent-child structure, stamped client_id + institution_id, no-template creates no OrgUnits). (AC-16)
- [x] 10.3 Enforce InstitutionType immutability on an Institution after creation; force archive-and-recreate to "change" type. — evidence: `repos/institution_repo.py::update_identity` (pops institution_type_id from update data); `test_template.py::TestInstitutionTypeImmutability` (institution_type_id not updated on identity update). (AC-16)
- [x] 10.4 Verify InstitutionType does NOT drive runtime module behavior (Attendance/Fees/Homework/Exams operate identically). — evidence: `test_template.py::TestInstitutionTypeStructuralOnly` (InstitutionType has only structural/template columns, no behavior fields; boundary-declaration stub noting full behavior-identical test deferred until business modules exist). (AC-16)

## 11. Ownership transfer transaction (D12, AC-11, AC-19)

- [x] 11.1 Implement the transfer request + both-client consent + Platform Owner approval flow (Q3 Approval). — evidence: `repos/approval_transfer_repo.py::approve_transfer` (checks consent_source + consent_dest, blocks if approval denied); `test_transfer.py::TestTransferApprovalFlow` (request creates pending approval, blocked without consent_source, blocked without consent_dest, blocked when denied). (AC-11, AC-19)
- [x] 11.2 Implement the single-transaction transfer updating `client_id` A→B across: Institution row, its OrgUnits, C-05 academic structure (coordinated with C-05, owned by C-05 — boundary), student records (C-02 scope), user-institution assignments (C-02 scope). — evidence: `repos/approval_transfer_repo.py::approve_transfer` (single-transaction Institution + OrgUnit client_id A→B, calls TransferCoordinator hooks); `services/transfer.py` (TransferCoordinator protocol + DefaultTransferCoordinator no-op stubs); `test_transfer.py::TestSingleTransactionTransfer` (all client_id A→B, partial failure rolls back, boundary hooks called). BOUNDARY: C-05/C-02 client_id migration hooks in place — C-05/C-02 plug in their own changes. (AC-11)
- [x] 11.3 Implement post-move isolation verification (the Institution and dependent data are accessible only under Client B). — evidence: `test_transfer.py::TestPostMoveIsolation` (Client A cannot access transferred Institution, Client B can). (AC-11)
- [x] 11.4 Implement `OwnershipTransferEvent` recording (`from_client`, `to_client`, `institution`, `approved_by`, `consent_source`, `consent_dest`, `transferred_at`, `reason`). — evidence: `repos/approval_transfer_repo.py::approve_transfer` (writes OwnershipTransferEvent); `test_transfer.py::TestOwnershipTransferEvent` (event row written with all D12 fields). (AC-11)
- [x] 11.5 Implement immutable-audit invariant: C-11 audit events recorded before the transfer keep their original ClientId. — evidence: `services/transfer.py::DefaultTransferCoordinator::preserve_audit_client_ids` (no-op — transfer MUST NOT rewrite audit client_ids); `test_transfer.py::TestImmutableAuditInvariant` (hook called, default is no-op, no audit_event table exists yet). BOUNDARY STUB: C-11 audit emission itself is Apply-D (task 13.4); the invariant (transfer MUST NOT rewrite pre-existing audit client_ids) is in place as a boundary hook. Full C-11 wiring in Apply-D. (AC-11, ADR §5 constraint 14)
- [x] 11.6 Implement user migration rules: users whose only Institution is the transferred one migrate to Client B; users with other Client-A Institutions stay in Client A and lose the transferred Institution. (Coordinated with C-02, owned by C-02 — boundary.) — evidence: `services/transfer.py::TransferCoordinator::migrate_users` (boundary hook, no-op default); `test_transfer.py::TestUserMigrationBoundary` (hook called, default is no-op). BOUNDARY: C-02 owns user-institution assignments + student records — hook in place, C-02 plugs in its own changes. Do NOT fake user migration. (D12)
- [x] 11.7 Note the billing-handoff coordination point (C-07/C-23 move subscription to Client B next billing cycle). This is a boundary declaration; C-07/C-23 own the billing behavior. — evidence: `services/transfer.py::TransferCoordinator::migrate_billing` (boundary hook, no-op default, called post-commit); `test_transfer.py::TestBillingHandoffBoundary` (hook called post-commit, default is no-op). BOUNDARY: C-07/C-23 own billing behavior — hook in place, no billing implemented. (D12)

## 12. Permission matrix wiring (C-04 boundary) (D11, A5, AC-15) — registered via the manifest `register_casbin_policies` hook

> C-01's manifest `register_casbin_policies(enforcer)` hook (A5) registers the D11 matrix as Casbin RBAC+ABAC policies. Casbin encoding is C-04's framework; C-01 supplies the matrix content AND registers its own policies (the app factory invokes the hook at startup).

- [ ] 12.1 Encode the D11 tiered-delegation matrix as Casbin RBAC+ABAC policies registered via the manifest `register_casbin_policies(enforcer)` hook (A5; C-04 owns the framework; C-01 supplies the matrix content). — evidence: policy definitions in `kernel/tenant_institution/` + tests for each role: Platform Owner ALL; Client Director own-client (cannot mutate the Client); Institution Admin own-institution (cannot mutate the institution); cross-institution roles READ-only. (AC-15, A5)
- [ ] 12.2 Verify cross-tenant writes (Client create/suspend/terminate, ownership transfer) are Platform-gated. — evidence: tests where a Client Director attempting these is rejected. (AC-15)
- [ ] 12.3 Verify all C-01 writes record actor identity via C-11. — evidence: tests asserting a C-11 audit event exists for each write. (AC-15)

## 13. Audit emission (C-11 boundary) (D8, D9, Q7, D12, AC-5, AC-6, AC-10, AC-11, AC-15)

- [ ] 13.1 Implement synchronous C-11 audit emission for Client lifecycle transitions (ClientId tagged). — evidence: a test asserting a C-11 event with ClientId on a transition. (AC-5)
- [ ] 13.2 Implement synchronous C-11 audit emission for Institution lifecycle transitions (ClientId + InstitutionId tagged). — evidence: a test asserting the event on a transition. (AC-6)
- [ ] 13.3 Implement C-11 audit emission for OrgUnit moves (generic `action="org_unit_moved"`, payload `{from_parent, to_parent, moved_by, ...}`). — evidence: a test asserting the event payload; assert no dedicated `org_unit_move_event` table. (AC-10)
- [ ] 13.4 Implement C-11 audit emission for ownership transfer + the immutability invariant across transfers. — evidence: tests as in 11.5. (AC-11)
- [ ] 13.5 Confirm no message broker / async event bus is introduced for C-01 (Q4 deferred). — evidence: codebase inspection asserting no broker integration; the emitter is synchronous. (Q4)

## 14. Boundary declarations (no cross-domain modifications) (D10, impact classification)

- [ ] 14.1 Verify C-01 has NO foreign key to C-05 (D10 zero-dependency invariant). — evidence: schema inspection test asserting no FK from any C-01 table to C-05 entities. (AC-18)
- [ ] 14.2 Verify `homeroom_teacher_id` is NOT a field on OrgUnit (it belongs to C-05 or C-02 RoleAssignment). — evidence: schema inspection test asserting the column's absence. (AC-18)
- [ ] 14.3 Verify tz/locale/currency/branding/`academic_year_start`/`grading_scale` are NOT intrinsic on Client or Institution (delegated to C-08). — evidence: schema inspection test asserting absence of those columns. (AC-17)
- [ ] 14.4 Verify no MODIFIED/REMOVED deltas exist for any other domain — every requirement is ADDED under `tenant-institution`. — evidence: inspection of `openspec/changes/add-c01-tenant-institution/specs/` confirming only `tenant-institution/spec.md` and only ADDED requirements.

## 14b. import-linter contract verification (A3, A4) — NEW task group

> Reinforces the dependency law (A3/A4) at the verification gate. The `.importlinter` config is added at scaffold time (task 1.4); this group asserts the contract holds as C-01 lands code.

- [ ] 14b.1 Run `lint-imports` against the `.importlinter` config (added in task 1.4) and confirm it passes — kernel (C-01) imports nothing from shared or business; C-01 exposes `services/` (published interfaces) while `repos/`/`models/` are not imported cross-module; graph is acyclic (A3, A4). — evidence: `lint-imports` output passing.
- [ ] 14b.2 Add a contract test asserting C-01 (`kernel/tenant_institution/`) imports nothing from `shared/` or `business/` (kernel→∅, A3). — evidence: the contract test passing.

## 15. Verification (gating apply → verify phase)

- [ ] 15.1 Run the OpenSpec verify phase (`openspec verify --change add-c01-tenant-institution`) once the apply phase lands the implementation. — evidence: verify output.
- [ ] 15.2 Run isolation tests covering AC-1 (cross-tenant data never visible) end-to-end. — evidence: test suite run with all isolation tests passing.
- [ ] 15.3 Confirm the c-01-explained supersede note — flag to the parent that `docs/platform-capabilities/c-01-tenant-institution-explained.md` should get a "Superseded by API contracts doc" note per Q5/PRD OQ-4 (this change does not edit `docs/`). — evidence: a note in the verify report.

## Open Questions (carried to apply)

- **OQ-1 — Tech stack (RESOLVED).** The platform tech-stack ADR locks Postgres+Supabase, Python+FastAPI, SQLAlchemy 2.0+Alembic, Supabase Auth JWT, Casbin, pytest; the platform software-architecture ADR locks the modular-monolith + module-manifest + monorepo structure (A1–A11). The former "stack TBD — confirm before implementation" gating tasks (old 1.1/1.2) are superseded by concrete scaffold tasks (new 1.1–1.4). No longer an open question.
- **OQ-2 — `c-01-explained` supersede note** (see task 15.3): parent schedules a small follow-up `docs/` edit per PRD OQ-4 / Q5. Not blocking apply; not done by this change.
- **OQ-3 — OpenSpec-git-discipline**: per task constraints, the worker does not run git commits. The parent runs the openspec-git-discipline flow (proposal commit before apply; merge-before-archive) before `/opsx-apply`.
- **OQ-4 — Deferred ADR items** (no action now): Q4 (event bus), Q8 (lifecycle-event partitioning), Q10 (temporal/`client_id` history), D2 UUID v7, D12 watermark. Revisited only if a concrete requirement emerges (ADR §7).