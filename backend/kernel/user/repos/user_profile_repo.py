"""DEPRECATED STUB — user_profile table was dropped in migration 022 (D6a).

This file exists so stale imports fail gracefully.
UserProfileRepository is no longer available.
"""

raise ImportError(
    "UserProfileRepository has been removed (migration 022, D6a). "
    "The user_profile table no longer exists. Human data lives on person."
)
